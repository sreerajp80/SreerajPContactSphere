# Plan — Update project to Flutter 3.44.8 / Dart 3.12.2

## Issue / Background
The installed toolchain is **Flutter 3.44.8 • Dart 3.12.2** (channel stable).
When running Gradle builds, Flutter prints warnings regarding older Gradle (8.12.0) and AGP (8.9.1) versions:
- Minimum Gradle version recommended by Flutter 3.44: **8.14.0**
- Minimum AGP version recommended by Flutter 3.44: **8.11.1**

`pubspec.yaml` currently specifies `environment.sdk: '^3.11.0'`, which trails Dart 3.12.2.

## Proposed Fixes
1. `pubspec.yaml`: `environment.sdk` -> `'^3.12.0'`
2. `android/gradle/wrapper/gradle-wrapper.properties`: Upgrade distribution URL to Gradle `8.14-all.zip`
3. `android/settings.gradle.kts`: Upgrade `com.android.application` plugin version to `8.11.1`
4. Update developer documentation (`CLAUDE.md`, `docs/guidelines/...`) to reflect Flutter 3.44 / Dart 3.12 requirements.
5. Verify via `flutter pub get`, `flutter analyze`, `flutter test`, and `flutter build apk --flavor prod --release`.
