# Change log: Split CLAUDE.md + add workflow rules

Implements [plans/20260625_072618_split-claude-md.md](../plans/20260625_072618_split-claude-md.md).

## What changed

- **CLAUDE.md** — rewritten to a slim, always-in-context version: project summary, commands,
  a short "Architecture at a glance" paragraph, a "Detailed docs" list linking the new docs with
  one-line hooks, and a new "Project workflow rules (mandatory)" section restating the global
  plans→approval→change_log rules for this repo.
- **docs/architecture.md** (new) — moved the layered-architecture detail and the SQLite schema
  section out of CLAUDE.md.
- **docs/known-gaps.md** (new) — moved the "early scaffold / missing pieces" section out.
- **docs/dependencies.md** (new) — moved the heavy-dependency / intended-features catalog out,
  regrouped by feature area.

## Result

CLAUDE.md is now small so it consumes minimal context per session; detailed material is one
`Read` away when actually needed. No code files were touched.
